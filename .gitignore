/auth.db
.DS_Store
/debug
/.env
/.envrc
/.media/
# Operator overlay — the folder is tracked (config/.gitkeep); the TOML is not
config/*.toml
# Complement run artifacts (regenerable; BASELINE.md is the record)
complement/baseline.jsonl
complement/work-*
complement/.test-status
